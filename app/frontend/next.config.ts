import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  /* config options here */

  images: {
    domains: ['source.unsplash.com', 'QmExampleHashForLaptopImage', 'ipfs.io', 'picsum.photos', 'images.unsplash.com'],
  },
}

export default nextConfig
